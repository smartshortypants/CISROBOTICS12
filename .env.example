# Server (Vercel) environment variables
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-3.5-turbo
# Optional: use Bing for live web results
BING_API_KEY=your_bing_key_here
# Optional: restrict CORS to your frontend origin
FRONTEND_ORIGIN=https://your-frontend.example.com

# Client (set as GitHub secret for Actions or environment when building)
# This should be the Vercel API base URL (no trailing slash). Example:
# NEXT_PUBLIC_API_URL=https://archeohub-xyz.vercel.app
NEXT_PUBLIC_API_URL=
